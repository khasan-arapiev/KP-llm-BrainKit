---
description: Boot the KP-God conductor: coordinate several parallel workstreams as live Claude Code lanes in WezTerm tabs, with a per-project board as memory. Windows only.
---

Invoke the `KP-God` skill. Read `~/.claude/skills/KP-God/SKILL.md` and follow it exactly. Right-size first: a single feature built start to finish is code-cowork inline, not orchestration. If the work is genuinely several independent streams, boot from the project's board (create it from the template if missing), split the work into file-disjoint lanes, spawn each lane with the scripts in `~/.claude/skills/KP-God/scripts/`, and stay the thin conductor: board current, watcher running, decisions batched.
