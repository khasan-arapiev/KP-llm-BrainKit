---
description: Retrofit an existing project into the KP+vault system. Catalogs credentials by reference, never copies values.
---

Invoke the `KP-Migrate` skill on the current working directory. Read `~/.claude/skills/KP-Migrate/SKILL.md` and follow it exactly. Dry-run preview is mandatory before any file is written. Credentials are catalogued by reference only, never copied as values into the vault.
