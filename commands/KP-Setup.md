---
description: Scaffold a new project folder and its vault pages using the KP-Setup skill.
---

Invoke the `KP-Setup` skill and follow Command 1 (KP setup) for the current directory or the project the user specifies. Read `~/.claude/skills/KP-Setup/SKILL.md` and follow the "Command 1: KP setup" section exactly. Ask only for inputs that cannot be inferred. Dry-run on non-empty folders before applying.
